spring:
  data:
    mongodb:
      port: 0
      database: test
      host: localhost
      #port: 27017
      #database: awesomeMongo

  shell:
    interactive:
      enabled: true
    noninteractive:
      enabled: false

    command:
      version:
        enabled: false
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2

mongock:
  runner-type: "ApplicationRunner" # default
#  runner-type: "InitializingBean"
  change-logs-scan-package:
    - ru.otus.hw.mongock.changelog
  mongo-db:
    write-concern:
      journal: false
    read-concern: local

logging:
  level:
    root: ERROR

#application:
#  data:
#    locale: ru_RU
#    authorsFileName: data/authors.json
#    booksFileName: data/books.json
#    commentsFileName: data/comments.json
#    genresFileName: data/genres.json
