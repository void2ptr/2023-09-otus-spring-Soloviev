spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5430/demoDB
    username: usr
    password: pwd
    replace: none
  flyway:
    enabled: false

jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
#        format_sql: true
    show-sql: false

logging:
  level:
    ROOT: ERROR
